{
  "name": "shieldtrack",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev:admin": "turbo run dev --filter=shield-admin",
    "dev:mobile": "turbo run start --filter=mobile",
    "dev:api": "turbo run dev --filter=api",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0"
  }
}